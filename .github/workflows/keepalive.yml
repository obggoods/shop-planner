name: Keep Supabase Awake

on:
  schedule:
    - cron: "0 3 * * 1" # 매주 월요일 03:00 UTC (한국시간 월요일 12:00)
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping public page
        run: |
          curl -sS -o /dev/null -w "%{http_code}\n" https://shop-planner.vercel.app/pricing
